# 2026-09-10 — Задача #242: Модульный рефакторинг lib/index.js и декомпозиция обработчиков маршрутов

## Контекст и цели
1. Файл `lib/index.js` разросся до более чем 1260 строк из-за инлайнового описания всей бизнес-логики подмаршрутов задач.
2. В проекте существует специализированный модуль `lib/routes.js`, предназначенный для чистых HTTP-обработчиков доски.
3. Провести рефакторинг с декомпозицией специализированных обработчиков задач в `lib/routes.js` без нарушения обратной совместимости API.
4. Обеспечить 100% прохождение тестов (652/652) и проверку совместимости с ядром.

## Выполненные изменения
- **Декомпозиция маршрутов в `lib/routes.js`**:
  - `handleCleanupWorktree`
  - `handleTaskDiff`
  - `handleTaskCommits`
  - `handleTaskMerge`
  - `handleTaskWorktreeStatus`
  - `handleTaskChecklist`
  - `handleTaskReport`
  - `handleGetTaskComments`
  - `handleAddTaskComment`
  - `handleAcceptTask`
  - `handleRejectTask`
  - `handleProgressDump`
- **Очистка `lib/index.js`**:
  - Замена монолитных инлайновых блоков на вызовы модульных функций с гарантированным закрытием запросов через `writeJson`.
  - Устранение дублирования кода проверки существования задачи и разбора параметров.
- **Quality Gates**:
  - `npm test`: 652/652 пройдены (включая строгий статический аудит импортов в `test/identity.test.mjs`).
  - `npm run compat`: расхождений нет.
  - Лимит размера пакета выдержан.
  - Версия обновлена до `0.2.4` (patch bump).