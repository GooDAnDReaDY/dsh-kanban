# Тестовая матрица dsh-kanban

## Модульные тесты

Идут без харнесса, без сети и без `node_modules`:

```bash
npm test
```

| Файл | Что покрывает |
|---|---|
| `test/config.test.mjs` | значения по умолчанию, скалярность всех полей настроек, метки и пределы колонок |
| `test/order.test.mjs` | дробный индекс: деление между соседями, тысяча вставок в одно место, сортировка обычным сравнением строк |
| `test/store.test.mjs` | хранилище: порядок, перенос без переписывания соседей, журнал переходов, поиск по сессии, переоткрытие базы |
| `test/routes.test.mjs` | сборка ответа доски, предел как предупреждение, отказы 404/400, защита от межсайтового запроса, разбор пути |
| `test/gitea.test.mjs` | клиент Gitea: токен только заголовком, отсев pull request, отказ до запроса при подделанном репозитории, тело ошибки наружу не выносится |
| `test/import.test.mjs` | перенос полей issue, различение репозиториев при пометке, обновление без сброса локальных полей, поведение без службы Gitea |
| `test/launcher.test.mjs` | первое сообщение, границы рабочей папки, порядок операций при запуске, откат при падении, выбор между живой, возобновлённой и новой сессией |
| `test/board-ui.test.mjs` | соседи при перетаскивании, разбор ответа доски, выбор слота при регистрации |
| `test/chip.test.mjs` | (плюс просьба показать задачу: читается один раз, открывает закрытую доску, будит открытую) |
| `test/chip.test.mjs` | получение сессии, регистрация чипа, штатное отсутствие задачи |
| `test/projecttask.test.mjs` | заведение issue и привязка карточки, создание репозитория, проверка имени до запроса, поведение при обрыве на середине |
| `test/intake.test.mjs` | разбор списка репозиториев, отбор issue для подхвата, память об отказанных, пороги и обратимость архива |
| `test/syncstate.test.mjs` | четыре положения сверки, сохранность времени последнего успеха при неудаче, причина в отчёте прохода, строка в шапке |
| `test/markdown.test.mjs` | разбор тела: заголовки, списки, код, ссылки; небезопасные схемы; пустое тело; маршрут отдаёт дерево |
| `test/filters.test.mjs` | разбор меток на пространства, порядок и счётчики отборов, складывание И/ИЛИ, переключение, обновление меток сверкой, те же правила в браузере |
| `test/time.test.mjs` | относительное время и его границы, будущее из-за расхождения часов, порог молчания, отметка входа в колонку и миграция старых баз |
| `test/boards.test.mjs` | наборы колонок по виду доски, откат неизвестного вида, превращение прежней доски в проектную без потери задач, отказ импортировать issue на простую, сворачивание колонок |
| `test/plan.test.mjs` | разбор и запись плана, границы указателя, отметки пунктов, состояние по живому агенту, строка карточки, инструмент `board_plan` |
| `test/commands.test.mjs` | таблица команд по колонкам, остановка идущего хода, отправка команды в чат задачи, запрет `done` инструменту, наличие обоих переводов |
| `test/transitions.test.mjs` | вывод колонки из наблюдений, приоритет источников, признак ожидания, поиск ветки и PR задачи, разрешение конфликтов |
| `test/sync.test.mjs` | сверка с Gitea на заглушке: один запрос на репозиторий, устойчивость к недоступности, признак ожидания по событиям |
| `test/twoway.test.mjs` | подпись вебхука, разбор события, план отправки, очередь с повторами, назначение меток идентификаторами |
| `test/identity.test.mjs` | совпадение имени пакета в трёх местах, порядок слотов, подписи колонок |

Заглушки вместо живых служб: Gitea и служба агентов подменяются объектами в тестах, реальных запросов и сессий не создаётся.

## Ручные сценарии на живом харнессе

| Сценарий | Ожидание |
|---|---|
| Установка с нуля | Плагин виден, карточка настроек на месте |
| Загрузка плагина | В журнале `dsh-web.service` нет строки `failed to apply loader entry dsh-kanban` |
| Совпадение имени пакета | Обе проверки curl ниже проходят |
| Раздел доски | «Задачи» открывается, шесть колонок в порядке воркфлоу |
| Ненастроенный Gitea | Импорт объясняет причину, а не показывает пустой список |
| Настроенный Gitea | Список открытых issue приходит, pull request в нём нет |
| Импорт issue | Карточка с заголовком, телом, метками и ссылкой |
| Повторный импорт | Уже импортированный issue помечен |
| Перетаскивание | Порядок держится после перезагрузки, в журнале запись с источником `manual` |
| Предел колонки | Превышение подсвечено, перенос выполнен |
| Запуск задачи | Чат с выбранной моделью, первое сообщение с текстом issue, карточка в «В работе» |
| Две задачи сразу | Две независимые сессии |
| Чип в чате | Виден в сессии с доски, отсутствует в обычной |
| Смена колонки из чипа | Отражается на доске и в журнале |
| Смена языка | Доска, карточка и чип перерисовываются без перезагрузки |
| Перезапуск харнесса | Задачи и порядок на месте |
| Выгрузка плагина | Маршруты сняты, база закрыта, ошибок в журнале нет |
| Автоперемещение | Закрытый issue без ветки уезжает в Done с пояснением в журнале |
| **Метки в самом Gitea** | После переноса карточки открыть issue в Gitea и убедиться, что метка колонки СТОИТ. Успешный код ответа этого не доказывает: назначение именами вместо идентификаторов проходит успешно и не делает ничего |
| Вебхук без подписи | Отвергается кодом 401 |
| Конфликт | Двинуть карточку на доске и одновременно изменить issue; проигравшая сторона названа в журнале задачи |

## Проверка совпадения имени пакета

Расхождение имени в трёх местах НЕ даёт ошибки в журнале: серверная половина работает, интерфейса нет.

```bash
curl -s http://127.0.0.1:3080/ | grep -c '@goodandready-private/dsh-kanban'
```

Ожидание: больше нуля.

```bash
curl -s -o /dev/null -w '%{http_code}\n' 'http://127.0.0.1:3080/plugins/@goodandready-private/dsh-kanban/client.js'
```

Ожидание: `200`.

## Проверка маршрутов

```bash
curl -s 'http://127.0.0.1:3080/dsh-kanban/board?board=main'
```

Ожидание: JSON с шестью колонками.

```bash
curl -s -o /dev/null -w '%{http_code}\n' -X POST -H 'sec-fetch-site: cross-site' 'http://127.0.0.1:3080/dsh-kanban/task'
```

Ожидание: `403`.

## Замер префиксных маршрутов

Префиксные маршруты регистрируются БЕЗ завершающего слэша: со слэшем
сопоставление сводится к равенству пути, и всё, что глубже, уходит в статику,
отвечая пустым телом. Пустое тело в ответе — верный признак, что маршрут не
сопоставился: свои отказы плагин отдаёт JSON.

```bash
for p in /dsh-kanban/task /dsh-kanban/task/abc/log /dsh-kanban/session/abc/task; do
  printf '%-32s -> ' "$p"
  curl -s -m 8 -w ' [%{http_code}]' "http://127.0.0.1:3080$p"; echo
done
```

Ожидание: у каждого пути тело JSON, а не пустая строка.

## Проверка после deploy

Установка из worktree релизной проверкой не считается: проверяется релизный артефакт. В issue фиксируются SHA `main`, версия, время, результаты health и smoke, состав релиза и cleanup.
